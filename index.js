// test-notify
// Repositório consumidor para validar a action notify-teams (sagui-oficial/actions).
// O workflow .github/workflows/notify-teams.yml dispara a notificação no Teams.

console.log("test-notify repo — see .github/workflows/notify-teams.yml");
